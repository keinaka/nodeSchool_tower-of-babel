var inputs = process.argv.slice(2);
    var result = inputs.map(/* ここにアロー関数を使って一文字だけ取得しましょう */(x)=>x.slice(0,1))
                       .reduce(/* ここにアロー関数を使って文字列結合しましょう */(a,b)=>a+b);
    console.log(result);
